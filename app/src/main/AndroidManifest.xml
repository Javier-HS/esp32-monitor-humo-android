<?xml version="1.0" encoding="utf-8"?>
<!--
    Manifiesto de la app Monitor de Humo ESP32.
    Declara permisos, actividades y servicios necesarios.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceder a internet (servidor AWS y Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permiso para vibrar el dispositivo en alertas -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Permiso para mostrar notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permiso para recibir notificaciones al reiniciar el dispositivo -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Permiso para mostrar alertas en pantalla completa -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Permiso para mostrar ventanas sobre otras apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MonitorHumo">

        <!-- Actividad principal — pantalla de monitoreo en tiempo real -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de alerta — emerge automáticamente al detectar humo -->
        <activity
            android:name=".AlertaActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop"/>

        <!-- Servicio Firebase — recibe notificaciones push en segundo plano -->
        <service
            android:name=".MiFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
